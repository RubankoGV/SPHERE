# Быстрая настройка проекта в Xcode

## Автоматическое создание проекта

Если у вас установлен [XcodeGen](https://github.com/yonaskolb/XcodeGen):

```bash
# Установите XcodeGen
brew install xcodegen

# Запустите скрипт
chmod +x create_xcode_project.sh
./create_xcode_project.sh
```

## Ручное создание проекта

### Шаг 1: Создайте новый проект

1. Откройте **Xcode**
2. **File** → **New** → **Project**
3. Выберите:
   - **Platform**: iOS
   - **Template**: App
4. Заполните:
   - **Product Name**: `SPHERE`
   - **Interface**: `SwiftUI`
   - **Language**: `Swift`
   - **Storage**: `None`
5. Выберите место сохранения (НЕ в папку SPHERE, а рядом с ней)
6. Нажмите **Create**

### Шаг 2: Удалите автоматически созданные файлы

1. Удалите `ContentView.swift` (если создан)
2. Удалите `SPHEREApp.swift` (мы используем свой)

### Шаг 3: Добавьте файлы проекта

1. В Xcode нажмите правой кнопкой на папку проекта
2. Выберите **Add Files to "SPHERE"...**
3. Перейдите в папку `SPHERE` (где находятся исходные файлы)
4. Выберите ВСЕ файлы и папки:
   - `SPHEREApp.swift`
   - `Models/`
   - `ViewModels/`
   - `Views/`
   - `Services/`
   - `Config/`
   - `Utilities/`
   - `Info.plist`
5. Убедитесь, что выбрано:
   - ✅ **Copy items if needed**
   - ✅ **Create groups**
   - ✅ **Add to targets: SPHERE**
6. Нажмите **Add**

### Шаг 4: Настройте Info.plist

1. Выберите проект в навигаторе
2. Выберите Target **SPHERE**
3. Перейдите в **Info** tab
4. Убедитесь, что все разрешения добавлены:
   - `NSHealthShareUsageDescription`
   - `NSHealthUpdateUsageDescription`
   - `NSCalendarsUsageDescription`
   - `NSRemindersUsageDescription`
   - `NSUserNotificationsUsageDescription`

### Шаг 5: Настройте Capabilities

1. Выберите Target **SPHERE**
2. Перейдите в **Signing & Capabilities**
3. Нажмите **+ Capability**
4. Добавьте:
   - ✅ **HealthKit**
   - ✅ **Background Modes** → включите:
     - Background fetch
     - Remote notifications

### Шаг 6: Настройте подпись

1. В **Signing & Capabilities**
2. Выберите ваш **Team** (или создайте Apple ID)
3. Xcode автоматически создаст Provisioning Profile

### Шаг 7: Проверьте настройки сборки

1. Выберите Target **SPHERE**
2. Перейдите в **Build Settings**
3. Найдите **Info.plist File**
4. Убедитесь, что путь: `SPHERE/Info.plist`

### Шаг 8: Соберите проект

1. Выберите симулятор или подключённое устройство
2. Нажмите **⌘B** (Build)
3. Если есть ошибки - исправьте их

### Шаг 9: Создайте Archive для AltStore

1. Выберите **Any iOS Device** в списке устройств
2. **Product** → **Archive**
3. Дождитесь завершения
4. В окне **Organizer**:
   - Выберите архив
   - Нажмите **Distribute App**
   - Выберите **Ad Hoc**
   - Выберите ваше устройство
   - Экспортируйте .ipa

## Готово! 🎉

Теперь у вас есть .ipa файл, который можно установить через AltStore!

