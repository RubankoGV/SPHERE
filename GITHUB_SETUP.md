# Инструкция по загрузке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name**: `SPHERE` (или любое другое имя)
   - **Description**: "Персональный трекер развития из 14 сфер жизни - iOS приложение"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (всё уже есть)
4. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

### Если репозиторий пустой (рекомендуется):

```bash
cd SPHERE
git remote add origin https://github.com/ВАШ_USERNAME/SPHERE.git
git branch -M main
git push -u origin main
```

### Если у вас уже есть файлы в репозитории:

```bash
cd SPHERE
git remote add origin https://github.com/ВАШ_USERNAME/SPHERE.git
git branch -M main
git push -u origin main
```

**Замените `ВАШ_USERNAME` на ваш GitHub username!**

## Шаг 3: Проверка

После выполнения команд:
1. Обновите страницу репозитория на GitHub
2. Все файлы должны быть загружены
3. README.md должен отображаться на главной странице

## Альтернативный способ через GitHub CLI

Если у вас установлен GitHub CLI:

```bash
cd SPHERE
gh repo create SPHERE --public --source=. --remote=origin --push
```

## Что уже готово:

✅ Git репозиторий инициализирован
✅ Все файлы добавлены в git
✅ Создан начальный коммит
✅ .gitignore настроен
✅ README.md создан

## Следующие шаги:

1. Создайте репозиторий на GitHub (см. Шаг 1)
2. Выполните команды из Шага 2
3. Готово! 🎉

## Полезные команды:

```bash
# Проверить статус
git status

# Посмотреть удаленные репозитории
git remote -v

# Обновить информацию о ветках
git fetch origin

# Создать новую ветку
git checkout -b feature/new-feature

# Отправить изменения
git push origin main
```

