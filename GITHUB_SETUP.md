# Инструкция по загрузке на GitHub

## ✅ Git репозиторий готов!

Локальный git репозиторий уже инициализирован и все файлы закоммичены.

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
4. Заполните форму:
   - **Repository name**: `SPHERE` (или любое другое имя)
   - **Description**: "Персональный трекер развития из 14 сфер жизни - iOS приложение"
   - Выберите **Public** или **Private**
   - **НЕ** ставьте галочки на:
     - ❌ "Add a README file" (уже есть)
     - ❌ "Add .gitignore" (уже есть)
     - ❌ "Choose a license"
5. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните эти команды в PowerShell:

```powershell
# Перейдите в папку проекта
cd C:\Users\Tyuiu\OneDrive\Desktop\SPHERE

# Добавьте удаленный репозиторий (замените ВАШ_USERNAME на ваш GitHub username)
git remote add origin https://github.com/ВАШ_USERNAME/SPHERE.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Важно:** Замените `ВАШ_USERNAME` на ваш реальный GitHub username!

Например, если ваш username `john-doe`, команда будет:
```powershell
git remote add origin https://github.com/john-doe/SPHERE.git
```

## Шаг 3: Авторизация

При первом `git push` GitHub может запросить авторизацию:
- **Personal Access Token** (рекомендуется) - создайте в Settings → Developer settings → Personal access tokens
- Или используйте GitHub Desktop для удобной работы

## Альтернативный способ: GitHub Desktop

Если у вас установлен GitHub Desktop:
1. Откройте GitHub Desktop
2. File → Add Local Repository
3. Выберите папку `C:\Users\Tyuiu\OneDrive\Desktop\SPHERE`
4. Нажмите "Publish repository"
5. Введите название и создайте репозиторий

## Что уже готово:

✅ Git репозиторий инициализирован  
✅ Все файлы добавлены в git  
✅ Создан начальный коммит  
✅ .gitignore настроен  
✅ README.md создан  
✅ Ветка переименована в `main`

## Проверка статуса:

Выполните команду для проверки:
```powershell
git status
git remote -v
```

## Полезные команды:

```powershell
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Посмотреть удаленные репозитории
git remote -v

# Отправить изменения
git push origin main

# Получить изменения
git pull origin main
```

## После загрузки:

1. Обновите страницу репозитория на GitHub
2. Все файлы должны быть видны
3. README.md отобразится на главной странице
4. Можете поделиться ссылкой на репозиторий!

---

**Готово!** 🎉 Теперь ваш код на GitHub!
